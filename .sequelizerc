const path = require("path");

module.exports = {
  config: path.resolve("./server/config", "config.json"),
  "migrations-path": path.resolve("./server/migrations", "migrations"),
  "models-path": path.resolve("./server/models", "models"),
  "seeders-path": path.resolve("./server/seeders", "seeders"),
};
